<%= form_with(model: vehicle) do |form| %>
  <% if vehicle.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vehicle.errors.count, "error") %> prohibited this vehicle from being saved:</h2>

      <ul>
        <% vehicle.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :name %>
    <%= form.text_field :name, class: "field" %>
  </div>

  <div class="mb-4">
    <%= form.label :capacity %>
    <%= form.text_field :capacity, class: "field" %>
  </div>

  <div>
    <%= form.submit class: "btn-primary float-right" %>
  </div>
<% end %>
