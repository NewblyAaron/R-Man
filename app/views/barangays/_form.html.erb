<%= form_with(model: barangay) do |form| %>
  <% if barangay.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(barangay.errors.count, "error") %> prohibited this barangay from being saved:</h2>

      <ul>
        <% barangay.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :name %>
    <%= form.text_field :name, class: "field" %>
  </div>

  <div class="mb-4">
    <%= form.submit class: "btn-primary float-right" %>
  </div>
<% end %>
